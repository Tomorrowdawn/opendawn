node_modules/
.tmp/
bun.lock
pnpm-lock.yaml

# install artifacts (symlinks)
.agents/
.claude/
.opencode/skills/

# python
__pycache__/

# issue time-tracking (local timesheet jsonl — NOT git history; avoid polluting log)
roadmap/issues/.timetracking/
